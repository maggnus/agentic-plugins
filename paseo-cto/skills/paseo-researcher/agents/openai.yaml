interface:
  display_name: "Paseo Researcher"
  short_description: "Investigate one scoped question read-only"
  default_prompt: "Use $paseo-cto:paseo-researcher to answer this question read-only with evidence."

policy:
  allow_implicit_invocation: false
