interface:
  display_name: "Paseo Reviewer"
  short_description: "Verify one Paseo outcome independently"
  default_prompt: "Use $paseo-cto:paseo-reviewer to challenge this outcome and return a source-linked verdict without changes."

policy:
  allow_implicit_invocation: false
