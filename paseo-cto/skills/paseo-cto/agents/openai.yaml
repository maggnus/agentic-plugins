interface:
  display_name: "Paseo CTO"
  short_description: "Run a fast Paseo engineering team"
  default_prompt: "Use $paseo-cto:paseo-cto to continue the plan: reconcile the agents, accept and land finished work, dispatch the next tasks, report one line."

policy:
  allow_implicit_invocation: true
