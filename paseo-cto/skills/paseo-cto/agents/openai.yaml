interface:
  display_name: "Paseo CTO"
  short_description: "Run a release-driven, durable Paseo fleet"
  default_prompt: "Use $paseo-cto:paseo-cto to advance the nearest shippable outcome, inspect returns by risk under the charter's review floor, keep the work tree and runtime truth current through the ledger, and report a quiet liveness line unless something material happened."

policy:
  allow_implicit_invocation: true
