interface:
  display_name: "Paseo Builder"
  short_description: "Implement one isolated Paseo task"
  default_prompt: "Use $paseo-cto:paseo-builder to build this task inside its zone, run checks in the foreground, commit locally and report."

policy:
  allow_implicit_invocation: false
